name: 'dbt_mini_project'
version: '1.0.0'
config-version: 2

profile: dbt_mini_project_profile

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"  
clean-targets:
    - "target"
    - "dbt_packages"

models:
  dbt_mini_project:
    staging:
      +materialized: view
      +snowflake_warehouse: pc_dbt_wh
    intermediate:
      +materialized: table
      +snowflake_warehouse: pc_dbt_wh_large
    marts:
      +materialized: table
      +snowflake_warehouse: pc_dbt_wh_large

dbt-cloud:
    project-id: 70471823569993
